
#ifndef __VECTOR_UTILS_H
#define __VECTOR_UTILS_H


#include <algorithm>
#include <iterator>
#include <vector>
#include <string>
#include <type_traits>
#include <functional>

using namespace std;

namespace vectorUtils {

// template <typename T>    
// auto find(vector<T>& items, std::function<bool(T a, T b)> predicate) {
//     return find_if(boughtProductsAndCount.begin(), boughtProductsAndCount.end(),
//                 predicate
//                 //    [&productName](const T& obj) {
//                 //        return obj.product.name == productName;
//                 //    }
//     );
// }

// // [](T a, T b){ return false; }


};  // namespace vectorUtils

#endif