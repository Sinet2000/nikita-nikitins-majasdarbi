#include <iostream>
#include <string>
using namespace std;

enum day {mds, asd};
enum month { HASD, ASD, ADS};
enum shortDay {te =2 , tsad};


int main() {
    bool isActive;

    cout << isActive;

    return 0;
}